name: Build and Deploy code

on: [push, pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: puling git repo
        uses: actions/checkout@v2
      - name: say hi to johnny nguyen
        run: echo "Hello Johnny Nguyen"
